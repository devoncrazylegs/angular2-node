import { ActionReducer, Action, State } from '@ngrx/store';

export const LATINIFY_WORD = 'Pig latinify a word';

const initialState:State = [];

export const pigLatinReducer: ActionReducer = (state = initialState, action: Action) => {

};
